now 比 me 多的欄位紀錄

"user_data"
	"sort"	integer DEFAULT 0,
	
"order_payment_data"
	"coin_discount"	decimal(10, 2) DEFAULT 0,
	"points_discount"	decimal(10, 2) DEFAULT 0,

"order_content_data"
	"temp_bill_lock"	char(1) DEFAULT 'N',
	
"terminal_data"	
	"use_call_num_date"	char(8),

"order_invoice_data"
	"platform_code"	varchar(30),

"company_invoice_params"
	"params"	TEXT,
	"platform_name"	varchar(100),

CREATE TABLE IF NOT EXISTS "modules_data" (
	"module_code"	varchar(30) NOT NULL,
	"module_params"	text,
	"active_flag"	char(1) DEFAULT 'N',
	"active_time"	DATETIME,
	"created_time"	timestamp,
	"updated_time"	timestamp,
	PRIMARY KEY("module_code")
);

